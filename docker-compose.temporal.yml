# Local Temporal stack (dedicated Postgres — not the tasktracker DB).
# Start: bash scripts/temporal-up.sh
# Manual: docker compose -f docker-compose.temporal.yml up -d
# UI: http://localhost:8233  |  gRPC: localhost:7233
# Env reference: https://github.com/temporalio/docker-compose
services:
  temporal-postgresql:
    container_name: temporal-postgresql
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: temporal
      POSTGRES_USER: temporal
    networks:
      - temporal-network
    ports:
      - "${TEMPORAL_DB_PORT:-5433}:5432"
    volumes:
      - temporal_pgdata:/var/lib/postgresql/data

  temporal:
    container_name: temporal
    depends_on:
      - temporal-postgresql
    environment:
      - DB=postgres12
      - DB_PORT=5432
      - POSTGRES_USER=temporal
      - POSTGRES_PWD=temporal
      - POSTGRES_SEEDS=temporal-postgresql
      - DYNAMIC_CONFIG_FILE_PATH=config/dynamicconfig/development-sql.yaml
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_CLI_ADDRESS=temporal:7233
    image: temporalio/auto-setup:1.29.1
    networks:
      - temporal-network
    ports:
      - "${TEMPORAL_FRONTEND_PORT:-7233}:7233"
    volumes:
      - ./temporal/dynamicconfig:/etc/temporal/config/dynamicconfig

  temporal-ui:
    container_name: temporal-ui
    depends_on:
      - temporal
    environment:
      - TEMPORAL_ADDRESS=temporal:7233
      - TEMPORAL_CORS_ORIGINS=http://localhost:8233
    image: temporalio/ui:2.34.0
    networks:
      - temporal-network
    # Image listens on 8080 inside the container; host uses TEMPORAL_UI_PORT (default 8233).
    ports:
      - "${TEMPORAL_UI_PORT:-8233}:8080"

networks:
  temporal-network:
    driver: bridge
    name: temporal-network

volumes:
  temporal_pgdata:
